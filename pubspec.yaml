name: app_ye_gestao_de_saude
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  google_fonts: ^6.2.1
  flutter:
    sdk: flutter

  


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  intl: ^0.18.0
  google_sign_in: ^6.2.1
  flutter_facebook_auth: ^6.2.0
  firebase_core: ^2.30.1
  cloud_firestore: ^4.17.2
  firebase_auth: ^4.19.4
  firebase_database: ^10.5.4
  uuid: ^4.4.0
  flutter_datetime_picker: ^1.5.1
  image_picker: ^0.8.4+4  # Adicionando a dependência para o pacote image_picker
  camera_camera: ^3.0.0  # Adicionando a dependência para o pacote camera_camera

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/Logo_gestao_em_saude.png
    - lib/assets/face.png
    - lib/assets/google.png
    - lib/assets/perfil.png
    - lib/assets/icone_homepage.png
    - lib/assets/icon_exames.png
    - lib/assets/icone_consultas.png
    - lib/assets/icone_medicamentos.png
